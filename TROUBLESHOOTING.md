# 故障排除指南

## 🔍 常见问题

### 1. 扩展未激活

**症状**：命令找不到，功能无法使用

**解决方案**：

1. **检查是否在扩展开发窗口**
   - 必须按 `F5` 后打开的新窗口
   - 不是主 Cursor 窗口

2. **检查工作区**
   - 确保已打开文件夹（工作区）
   - 不是只打开单个文件

3. **查看激活日志**
   - 打开输出面板（`Ctrl+Shift+U`）
   - 选择 "CodeMind" 输出通道
   - 查看是否有激活日志

4. **重新加载窗口**
   - `Ctrl+R` 或 `Cmd+R`
   - 或关闭窗口重新按 `F5`

---

### 2. 命令找不到

**症状**：在命令面板中找不到 CodeMind 命令

**解决方案**：

1. **使用命令面板**
   - `Ctrl+Shift+P`（Windows/Linux）
   - `Cmd+Shift+P`（Mac）
   - 输入 "CodeMind" 查看所有命令

2. **检查扩展是否激活**
   - 查看输出面板的激活日志
   - 确保看到 "扩展已完全激活" 消息

3. **重新编译**
   ```bash
   npm run compile
   ```
   - 然后重新按 `F5` 启动

---

### 3. 记忆没有自动提取

**症状**：保存文件后没有自动提取记忆

**解决方案**：

1. **检查配置**
   - `Ctrl+,` 打开设置
   - 搜索 "memoryManager.autoExtract"
   - 确保为 `true`

2. **检查文件类型**
   - 只处理源代码文件
   - 支持：.go, .js, .ts, .py, .java, .rs, .cpp, .c, .cs, .php, .rb

3. **检查文件路径**
   - 不在 node_modules、.git、dist 等目录
   - 这些目录会被自动忽略

4. **查看输出日志**
   - 打开输出面板
   - 查看是否有错误信息

---

### 4. 验证没有显示

**症状**：代码验证没有显示诊断标记

**解决方案**：

1. **检查配置**
   - `memoryManager.autoValidate` 是否为 `true`
   - 默认已启用

2. **检查是否有记忆**
   - `CodeMind: View All Memories`
   - 确保有相关记忆

3. **检查诊断面板**
   - `Ctrl+Shift+M` 打开诊断面板
   - 查看是否有验证问题

4. **手动验证**
   - `CodeMind: Verify Code Against Memory`
   - 查看是否有问题

---

### 5. 记忆注入失败

**症状**：记忆没有注入到文件或 Cursor API

**解决方案**：

1. **检查 Cursor API**
   - `CodeMind: Check Cursor API Availability`
   - 查看可用方法

2. **检查文件权限**
   - 确保有写入权限
   - 检查 `.cursorrules` 文件是否可写

3. **手动注入**
   - `CodeMind: Inject Memories to Current File`
   - 查看是否成功

---

### 6. 编译错误

**症状**：`npm run compile` 失败

**解决方案**：

1. **检查 Node.js 版本**
   ```bash
   node --version
   ```
   - 需要 Node.js 14+

2. **重新安装依赖**
   ```bash
   rm -rf node_modules
   npm install
   ```

3. **检查 TypeScript 错误**
   - 查看编译输出的错误信息
   - 修复代码错误

---

### 7. PowerShell 执行策略错误

**症状**：Windows 上 `npm run compile` 失败，提示执行策略错误

**解决方案**：

**方法 1：使用 CMD**
```bash
# 在 CMD 中运行
npm run compile
```

**方法 2：修改 PowerShell 执行策略**
```powershell
# 以管理员身份运行 PowerShell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**方法 3：使用 CMD 编译任务**
- 使用 `.vscode/tasks.json` 中的 "npm: compile (CMD)" 任务

---

### 8. 扩展命令在终端中找不到

**症状**：在终端中运行扩展命令失败

**解决方案**：

**扩展命令不是终端命令！**
- 扩展命令通过命令面板（`Ctrl+Shift+P`）运行
- 不是在终端中运行
- 使用命令面板输入命令名称

---

### 9. 记忆丢失

**症状**：之前保存的记忆不见了

**解决方案**：

1. **检查存储路径**
   - 默认：`.cursor-memory/memories.json`
   - 在工作区根目录

2. **检查文件是否存在**
   - 查看 `.cursor-memory` 目录
   - 检查 `memories.json` 文件

3. **恢复备份**
   - 如果有导出文件，可以导入
   - `CodeMind: Import Memories`

---

### 10. 性能问题

**症状**：扫描项目很慢，或扩展卡顿

**解决方案**：

1. **限制扫描范围**
   - 确保 `.gitignore` 正确配置
   - 排除大型目录（node_modules、vendor 等）

2. **减少记忆数量**
   - 清理过期记忆
   - `CodeMind: Clean Expired Memories`

3. **禁用自动功能**（临时）
   - 设置 `autoExtract` 为 `false`
   - 设置 `autoValidate` 为 `false`

---

## 🐛 调试技巧

### 查看日志

1. **输出面板**
   - `Ctrl+Shift+U` 打开输出面板
   - 选择 "CodeMind" 输出通道

2. **调试控制台**
   - 按 `F5` 启动调试
   - 查看调试控制台的日志

3. **开发者工具**
   - `Help → Toggle Developer Tools`
   - 查看控制台错误

### 收集诊断信息

如果问题持续，收集以下信息：

1. **扩展版本**
   - 查看 `package.json` 中的版本号

2. **Node.js 版本**
   ```bash
   node --version
   npm --version
   ```

3. **编译输出**
   ```bash
   npm run compile > compile.log 2>&1
   ```

4. **激活日志**
   - 复制输出面板中的所有日志

5. **配置文件**
   - `package.json`
   - `.vscode/launch.json`
   - `.vscode/tasks.json`

---

## 📞 获取帮助

如果以上方法都无法解决问题：

1. **查看文档**
   - [README.md](README.md)
   - [FEATURES.md](FEATURES.md)

2. **提交 Issue**
   - [GitHub Issues](https://github.com/your-username/cursor-memory-manager/issues)
   - 包含诊断信息

3. **社区讨论**
   - [GitHub Discussions](https://github.com/your-username/cursor-memory-manager/discussions)

---

**希望这些信息能帮助你解决问题！** 🎉

